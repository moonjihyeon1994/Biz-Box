<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.bizbox.mapper.populationByBusinessMapper">
	
	<select id="getPopulation" parameterType="String" resultType="pb">
		select * from populations_business where trdar_cd = #{trdar_cd}
	</select>
	
</mapper>